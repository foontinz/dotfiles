# Toggle experimental mode to test new features
experimental: false
registries:
  - name: foontinz/touch-bistro-registry
# - name: TouchBistro/tb-registry-example
# Custom playlists
# Each playlist can extend another playlist as well as define its services
playlists:
# db:
# services:
# - postgres
# dev-tools:
# extends: db
# services:
# - localstack
# Override service configuration
# Overrides must use the full service name with the registry name.
overrides:

# TouchBistro/tb-registry-example/postgres:
# mode: remote
# remote:
# tag: feat/new-version
